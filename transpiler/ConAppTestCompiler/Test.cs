
//opath=F:/doxnoor.js , ab=true

using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using CSharp.Wrapper.JS;
using CSharp.Wrapper.Angular;
using ConAppTestCompiler;


namespace ConAppTestCompiler.mac.jac
{
    [Angular(ModuleName = "app", ActionType = "factory", ActionName = "test", DI = "$scope,$ionicModal")]
    public class Test<T> : CSharp.Wrapper.JS.jsClass
    {
         Func<int, string> dx00723 = null;
        private Func<int, string> dx007 = null;
        enum SX { max, jax, pcs, WOX=100 };
        Dictionary<string, Dictionary<string, object>> dic = new Dictionary<string, Dictionary<string, object>>();
        private Utils.Pager<Student> pager = new Utils.Pager<Student>(20);
        private int x = 1;
        int asd = 23;
        Student s = new Student { Name = "Jasin" };
        public int[] dfers = new[] { 3, 4, 5, 6 };
        //ConAppTestCompiler.mac.jac.Test<Student> nas = new ConAppTestCompiler.mac.jac.Test<Student>();
        public virtual string _s_Macdonal(string asd,int _s_man=34, int werr=0)
        {
            
            int xsa = 1 == 1 ? 12 : 567;
            List<Student> listws = new List<Student>();
            var masd=(from u in listws select new Student{ Id=u.Id, Address=u.Id==0?u.Address:"Jamurki?id="+u.Id }).ToList();
            int xs = _s_man;
            Dictionary<string, string> dx = new Dictionary<string, string>();
            
            dx.Add("asd", "wertfg");
            dx["asdf"] = "asdd";
            dx.Keys.ForEach((x, i) => { console.log(x); });
            List<KeyValuePair<string, string>> list = dx.Where(x => x.Key == "mas") as List<KeyValuePair<string, string>>;
            List<KeyValuePair<string, string>> listsd = (List<KeyValuePair<string, string>>)dx.Where(x => x.Key == "mas");
            dx.ForEach((x, e) => { console.log(x.Key); });
            dx.Remove("as");
            dx.Clear();
            return "";
        }
        public override string ToString()
        {
            _s_Macdonal("");
            Dictionary<string, Dictionary<string, object>> dicp = new Dictionary<string, Dictionary<string, object>>();
            HTMLElement ht = new HTMLElement { };
            ht.classx = "dx-fer".ToString();
            int s = 0;
            return ToString();
        }
        public System.Collections.Generic.List<Student> dx(System.Collections.Generic.List<Student> smax, Utils.Pager<Student> spager)
        {
            return null;
        }
        public Test(int x, int b, int c, IScope _s_scope)
        {
            int[] dfers = new[] { 3, 4, 5, 6 };
            //Test<Student> mac = new Test<Student>();
            new SQLight().query("delete from students");
            int[] dfer = new[] { 1 };
            alert(new[] { 3, 4, 5, 6 }.Join("-"));
            new Utils.Pager<Student>(20).first();
        }
        public void doSmth(List<Student> list, Dictionary<string, Dictionary<string, object>> dic)
        {
            List<int> listx = new List<int> { 1, 2, 3, 4, 5, 6, 7, 8 };
            List<int> s = new List<int>();
            List<int> d = new List<int>() { 1, 2, 3, 4, 5, 6, 7, 8 };
            List<int> sde = new List<int> { };
            List<int> sdeaa = new List<int>() { };
        }
        #region MyRegion
        public System.Collections.Generic.List<Student> tdx(System.Collections.Generic.List<Student> smax, Utils.Pager<Student> spager)
        {
            return null;
        }
        public System.Collections.Generic.Dictionary<string, Dictionary<string, object>> sderft(System.Collections.Generic.List<Student> smax, Utils.Pager<Student> spager)
        {

            return null;
        }
        #endregion
    }
}
